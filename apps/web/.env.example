# Worker API のデプロイ先 URL（wrangler deploy 後に取得）
NEXT_PUBLIC_API_URL=https://your-worker.your-subdomain.workers.dev

# API 認証キー（wrangler secret put API_KEY に設定したのと同じ値）
# ローカル開発時のみ。本番では sessionStorage 経由でログイン後に取得される
NEXT_PUBLIC_API_KEY=
